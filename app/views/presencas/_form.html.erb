<%= form_with(model: presenca, local: true) do |form| %>
  <% if presenca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(presenca.errors.count, "error") %> prohibited this presenca from being saved:</h2>

      <ul>
        <% presenca.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :compareceu %>
    <%= form.text_field :compareceu %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
