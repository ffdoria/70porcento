<%= form_with(model: [@subject,@atividade], local: true) do |form| %>
  <% if @atividade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(atividade.errors.count, "error") %> prohibited this atividade from being saved:</h2>

      <ul>
        <% @atividade.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :data_at %>
    <%= form.date_select :data_at, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :horario_at %>
    <%= form.time_select :horario_at, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :tipo %>
    <%= form.text_field :tipo, class:"form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :comentarios %>
    <%= form.text_area :comentarios, class:"form-control" %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
