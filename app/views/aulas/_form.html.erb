<%= form_with(model: [@subject,@aula], local: true) do |form| %>
  <% if @aula.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aula.errors.count, "error") %> prohibited this aula from being saved:</h2>

      <ul>
        <% @aula.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :data_aul %>
    <%= form.date_select :data_aul %>
  </div>

  <div class="field">
    <%= form.label :horario_aul %>
    <%= form.time_select :horario_aul %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
